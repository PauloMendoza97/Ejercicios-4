#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,c;
    c=getchar();
    while (c!='\n')
    {
        for (i = 0; i<lim-1; ++i)
        {
            if(c!=EOF) s[i] = c;
        }
    }
    return 0;
}
